@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Sự cố xảy ra.</h1>
<h2 class="text-danger">Có lỗi xảy ra trong quá trình xử lý.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}